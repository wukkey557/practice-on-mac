
/*
快速合并算法
连通性问题:
*/


#include <stdio.h>

#define N 10000

int main() {
	int i, p, q, t, id[N];
	for (i = 0; i < N; i++) {
		id[i] = i;
	}

	while (scanf("%d %d", &p, &q) == 2) {
		

		printf("%d %d\n", p, q);
	}

	return 0;
}